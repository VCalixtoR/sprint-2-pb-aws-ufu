<div id="top-container" class="container-fluid">
  <h1 id="main-title" class="text-center">Handwrytten API - cartões à sua escolha</h1>
  <p id="main-subtitle" class="text-center">Esta é a API Handwrytten para enviar cartões escritos com a caligrafia de sua escolha. Usando esta API, você pode enviar cartões para usuários. </p>
  <form id="search-form" class="form-inline" action="/card"  method="post">
    <div class="form-group col-md-10">
      <input type="text" class="form-control" id="card_id" value="" name="card_id" placeholder="Digite o ID do cartão">
    </div>
    <div class="col-md-2">
      <button type="submit" class="btn btn-primary">Procurar</button>
    </div>
  </form>
</div>
<main>
  <div id="jobs-container" class="container">
    <div class="row">
      <div class="col-md-12">
        {{#if search}}
        <h2 id="job-list-title">Cartão {{search.card_id}}</h2>
        {{else if category}}
        <h2 id="job-list-title">Cartões disponíveis</h2>
        {{else}}
        <h2 id="job-list-title">Digite um código válido</h2>
        {{/if}}
        <ul id="job-list" class="list-group">
          {{#if data}}
          <li class="list-group-item new-job">
            <img src="{{data.card.cover}}" alt="Cover">
            {{!-- <p>{{dataValues.company}}</p> --}}
            <h2>{{data.card.name}}</h2>
            <p>Preço: ${{data.card.price}}</p>
            <p>Tamanho: {{data.card.details_size}}</p>
            <p>Autor: {{data.card.details_author}}</p>
          </li>
          {{/if}}
        </ul>
        
        <ul id="job-list" class="list-group">
          {{#if category}}
            {{#each (limit category.cards 4)}}
            <li class="list-group-item new-job">
            <img src="{{this.cover}}" alt="Cover">
            <h2>{{this.name}}</h2>
            <p>Preço: ${{this.price}}</p>
            <p>ID cartão: {{this.id}}</p>
            <p>Tamanho: {{this.details_size}}</p>
            <p>Autor: {{this.details_author}}</p>
            </li>
            {{/each}}
          {{/if}}
        </ul>
      </div>
    </div>
  </div>
</main>